# @formbox/storybook

Storybook package for Formbox Renderer.

## Run locally

```bash
pnpm --filter @formbox/storybook dev
```

Build:

```bash
pnpm --filter @formbox/storybook build
```

## Output paths

- Storybook: `dist/`
